pub fn parse_capabilities(cap_string: &str) -> std::collections::HashMap<String, String> {
    // Stub for capabilities parser
    let mut map = std::collections::HashMap::new();
    map.insert("parsed".to_string(), cap_string.to_string());
    map
}
